import React from 'react';

import HomePage from '../HomePage';

/**
 * Main component for the usePopcorn app.
 * Holds the homepage (which is the entirety of the app as of now)
 */
export default function App() {
  return <HomePage />;
}
